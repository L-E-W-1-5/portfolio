import React from 'react';

export function CV() {
    return (
        <div>
            <h4>CV</h4>
            <p>Maybe a link to an image of it... or is that lazy?</p>
            <p>Have a file containing an array of objects, then map each of my jobs in a component?</p>
            <p>Thin about it at a later date</p>
        </div>
    )
}